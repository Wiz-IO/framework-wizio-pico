#ifndef MINMAX_H_
#define MINMAX_H_

#ifndef min
#define min( a, b ) ( (a) < (b) ? (a) : (b) )
#endif // min

#ifndef max
#define max( a, b ) ( (a) > (b) ? (a) : (b) )
#endif // max

#endif /* MINMAX_H_ */
